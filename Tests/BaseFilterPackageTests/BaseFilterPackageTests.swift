import XCTest
@testable import BaseFilterPackage

final class BaseFilterPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(BaseFilterPackage().text, "Hello, World!")
    }
}
